## 13. FEATURE GATE LOGIC

### Implementáció

```typescript
// Pseudocode
function canAccessFeature(user, feature) {
  if (user.subscription_plan === 'PREMIUM') {
    return true;
  }
  
  switch(feature) {
    case 'journal_unlimited':
      return false; // FREE: max 3/week
      
    case 'habit_unlimited':
      return false; // FREE: max 1 habit
      
    case 'video_premium':
      return false; // FREE: only free videos
      
    case 'strength_quiz_repeat':
      return false; // FREE: 1x only
      
    case 'export_data':
      return false; // PREMIUM only
      
    default:
      return true; // Everything else free
  }
}
```

### Upgrade Prompts

**Trigger points:**

- 3. journal entry attempt (Free user)
- 2. habit create attempt
- Premium video click
- Quiz repeat attempt

**Modal:**

- "Ez a funkció csak Prémium tagoknak! 🌟"
- Features lista
- "Előfizetek most" button

---